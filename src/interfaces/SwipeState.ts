export interface SwiperState {
  currentIndex: number;
  currentPosition: number;
  initialized: boolean;
  swiperWidth: number;
  slidesScrollWidth: number;
  slidesLoaded: boolean;
  noTranslate: boolean;
}
